import SimpleObject from "./SimpleObject.js";import Sound from "./../Sound.js";export defaultclass ItemWeapon extends SimpleObject {	constructor(g, x, y, itemId) {		super(g, x, y);				// sprite		this.texture = g.resources.items.textures['item-' + (itemId - 1) + '.png'];				// properties		this.itemId = itemId;		this.respawnTime = 30;	}	handleCollisions(player) {		var that = this;		super.handleCollisions(player, function(player){			player.addWeapon(that.itemId);			Sound.play("wpkup");			that.sprite.visible = false;			return true;		});	}}